{
  "name": "rootstock",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "hasura:start": "bun run hasura/scripts/start.ts",
    "hasura:migrate": "bun run hasura/scripts/migrate.ts",
    "hasura:migrate:new": "bun run hasura/scripts/migrate.ts new",
    "hasura:migrate:rollback": "bun run hasura/scripts/migrate.ts rollback",
    "hasura:migrate:status": "bun run hasura/scripts/migrate.ts status",
    "hasura:introspect": "bun run hasura/scripts/introspect.ts",
    "hasura:deploy": "bun run hasura/scripts/deploy.ts",
    "hasura:setup-hosts": "sudo bun run hasura/scripts/setup-hosts.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.980.0",
    "@aws-sdk/client-s3": "^3.980.0",
    "@aws-sdk/client-ssm": "^3.980.0",
    "@pulumi/aws": "^7.19.0",
    "@pulumi/pulumi": "^3.220.0",
    "@pulumi/random": "^4.16.0"
  }
}